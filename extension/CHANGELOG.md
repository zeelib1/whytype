# Changelog

## 0.0.1 (unreleased)

- "WhyType: Why does this fail?" quick fix on TypeScript/JavaScript
  diagnostics — renders tsserver's elaboration as a because-chain in a
  webview panel.
- Command-palette variant that picks up the diagnostic at the cursor.
